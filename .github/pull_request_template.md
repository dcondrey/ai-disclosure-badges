**What does this change?**

**Why?**

**Checklist**
- [ ] If this touches `index.html`, I opened it in a browser and verified the generator still works (no console errors, badges render, snippets copy correctly)
- [ ] If this touches `badge-logic.js`, `node --test` passes
- [ ] If this changes a badge URL or color, I verified it resolves (`curl -sI <url>` returns 200)
- [ ] If this changes wording in `docs/`, it stays consistent with the current spec at [w3c-cg/ai-content-disclosure](https://github.com/w3c-cg/ai-content-disclosure)
- [ ] No new dependencies or build tooling introduced
